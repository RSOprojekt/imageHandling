kumuluzee:
  name: image-handling-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8080
    http:
      port: 8080
  datasources:
    - jndi-name: jdbc/ImageHandlingDS
      connection-url: jdbc:postgresql://localhost:65432/image-data
      username: dbuser
      password: postgres
      max-pool-size: 20
rest-properties:
  dev-mode: false